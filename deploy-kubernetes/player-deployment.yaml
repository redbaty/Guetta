apiVersion: apps/v1
kind: Deployment
metadata:
  name: player-deployment
  namespace: guetta
spec:
  selector:
    matchLabels:
      app: player
  template:
    metadata:
      labels:
        app: player
    spec:
      containers:
      - name: player
        image: redbaty/guetta:player-latest
        imagePullPolicy: Always
        envFrom:
          - configMapRef:
              name: guetta-player-envs
        resources:
          requests:
            memory: "128Mi"
            cpu: "1000m"
          limits:
            memory: "512Mi"
            cpu: "2000m"
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: guetta-player-envs
  namespace: guetta 
data:
  ASPNETCORE_ENVIRONMENT: "Development"
  TOKEN: "ODU0NTMyODkyNDE2MTQ3NTI2.YMlTxg.ANgMnCJFbUZgRmhVeTvccWhwfgg"
  REDIS: "redis-service.guetta"